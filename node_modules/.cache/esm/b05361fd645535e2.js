let express,GoogleGenerativeAI;_645‍.w("express",[["default",["express"],function(v){express=v}]]);_645‍.w("@google/generative-ai",[["GoogleGenerativeAI",["GoogleGenerativeAI"],function(v){GoogleGenerativeAI=v}]]);


const app = express();

app.use(express.json());

const genAI = new GoogleGenerativeAI("AIzaSyCStV0Frm5aKwUeMzwwS7AODZS64Se3xvQ");

app.post('/api/process-form', async (req, res) => {
  const { sufferIssue, purposeOfSuffer, ageRadio } = req.body;

  // Use the form data to generate a prompt for the AI model
  const prompt = `how are you`;

  const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash" });
  const result = await model.generateContent(prompt);
  const response = await result.response;
  const text = response.text();

  res.json({ text });
});

app.listen(3000, () => {
  console.log('Server listening on port 3000');
});